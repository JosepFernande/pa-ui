{
  "name": "pa-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/pa-ui/src",
  "prefix": "pa",
  "projectType": "library",
  "release": {
    "version": {
      "generatorOptions": {
        "packageRoot": "dist/{projectRoot}",
        "currentVersionResolver": "git-tag",
        "fallbackCurrentVersionResolver": "disk"
      }
    }
  },
  "tags": ["type:umbrella"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "inputs": ["production", "^production", "{workspaceRoot}/README.md"],
      "options": {
        "commands": [
          "mkdir -p dist/libs/pa-ui",
          "cp libs/pa-ui/package.json dist/libs/pa-ui/package.json",
          "cp libs/pa-ui/src/index.mjs dist/libs/pa-ui/index.mjs",
          "cp README.md dist/libs/pa-ui/README.md"
        ],
        "parallel": false
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/pa-ui/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
