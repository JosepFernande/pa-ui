---
'@pa-ui/core': patch
---

Add `--pa-input-*` component-token defaults to `theme.css`, consumed by the new
`PaInput` component. Input dimensions are assistant-authored provisional values
pending design validation.
