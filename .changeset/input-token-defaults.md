---
'@pa-ui/core': minor
---

Add `--pa-input-*` component-token defaults to `theme.css`, consumed by the new
`PaInput` component (per-size radius tokens, focus border). Input dimensions are
assistant-authored provisional values pending design validation.
